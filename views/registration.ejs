<!DOCTYPE html>
<html lang="en" class="html">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BS Register</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="shortcut icon" href="/logo-and-icons/logo-website.png">

    <style>
        .html {
            font-family: 'Poppins', sans-serif;
        }
        
        .body{
            margin: 0;
        }

        .page {
            min-height:100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .container {
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            background-color: #F0F0F0;
            width: 350px;
            height: 450px;
            border-radius: 20px;
            box-shadow: 0 0 20px gray;
        }

        .tittle {
            text-align: center;
        }

        .input-field {
            margin: 0 75px;
        }

        .input-field--text {
            margin: 0 0 10px;
            height: 30px;
            width: 200px;
            border-radius: 10px;
        }

        .input-field--email {
            margin: 0 0 10px;
            height: 30px;
            width: 200px;
            border-radius: 10px;
        }

        .input-field--password {
            margin: 0 0 10px;
            height: 30px;
            width: 200px;
            border-radius: 10px;
        }

        .button {
            margin: 10px 0;
            height: 30px;
            width: 70px;
            border-radius: 10px;
            cursor: pointer;
        }

        .logo {
            position: absolute;
            background-image: url(/logo-and-icons/logo-website.png);
            height: 60px;
            width: 60px;
            background-size: cover;
            background-position: center;
            margin: 7.5px 0 7.5px 50px;
        }

        .reg-text {
            font-size: 14px;
            padding-left: 7px;
        }

        .doYouHaveAnAcc {
            font-size: 12px;
        }
    </style>
</head>
<body class="body">
    <div class="logo"></div>
    <div class="page">
        <div class="container">
            <h2 class="tittle">REGISTER</h2>
            <div class="input-field">
               <form action="/register" method="POST" >
                <label class="reg-text">Fullname</label>
                <input 
                type="text"
                name="username"
                class="input-field--text"
                placeholder="Type your Fullname.."
                required
                >
                <br>
                <label class="reg-text">Email</label>
                <input 
                type="email"
                name="email"
                class="input-field--email"
                placeholder="Type your email.."
                required
                >
                <br>
                <label class="reg-text">Password</label>
                <input 
                type="password"
                name="password"
                class="input-field--password"
                placeholder="Type your password.."
                required
                >
            </div>
            <a href="/redirectToLogin" class="doYouHaveAnAcc">I already have an account</a>
            <button class="button" type="submit">Sign Up</button>
        </form>
        </div>
    </div>
</body>
</html>